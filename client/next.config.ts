import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-4e20f293-morphvm-tjwb9tdm.http.cloud.morph.so'],
};

export default nextConfig;